  <!-- Top menu -->

<nav class="navbar navbar-expand-lg navbar-dark bg-dark navbar-custom">
  <div class="container">
  <% if current_user %>
           <a class="navbar-brand" href="/dashboard">Do Things!</a>
        <% else %>
           <a class="navbar-brand" href="/">Do Things!</a>
        <% end %>
        
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                    <a class="nav-link scroll-link" href="/"></a>
            </li>

  
            </div>
            <% if current_user %>
              <li class="nav-item">
                <a class="nav-link scroll-link" href="/users/<%= current_user.id %>">My Profile</a>
              </li>
              <li class="nav-item">
                <a class="nav-link scroll-link" href="/sign_out">Sign Out</a>
              </li>
              <% else %>
              <li class="nav-item">
                <a class="nav-link scroll-link" href="/sign_in">Sign In</a>
              </li>
              <li class="nav-item">
                <a class="nav-link scroll-link" href="/sign_up">Sign Up</a>
              </li>
              <li class="nav-item">
                <a class="nav-link scroll-link" href="/auth/google_oauth2">Sign In With Google</a>
              </li>
              <% end %>
          </ul>
        </div>
   </div>
</nav>